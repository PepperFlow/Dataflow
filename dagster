from dagster import job, op
import subprocess

@op
def extract():
    subprocess.run(["python", "dlt_pipeline/load_job_ads_dlt.py"])

@op
def transform():
    subprocess.run(["dbt", "run", "--project-dir", "dbt_project"])

@job
def etl_job():
    extract()
    transform()
